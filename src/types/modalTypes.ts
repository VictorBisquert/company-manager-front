//NOTE: Aqui declaramos el tipo o fromato que va a recibir el modal

/*
export interface ModalProps {
  modalIsOpen: boolean;
  setModalIsOpen: (isOpen: boolean) => void;
  selectedData?: any;
  idInstallation?: string;
}
*/